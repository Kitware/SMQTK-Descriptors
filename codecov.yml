codecov:
  strict_yaml_branch: master

coverage:
  status:
    project:
      default: false
      tests:
        target: 90%
        paths:
          - "tests/"
        if_not_found: success
      package:
        target: 60%
        paths:
          - "smqtk_descriptors/"
    patch:
      default: false
      tests:
        target: 100%
        paths:
          - "tests/"
        if_not_found: success
      package:
        target: 80%
        paths:
          - "smqtk_descriptors/"
