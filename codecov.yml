coverage:
  status:
    project:
      default: false
      tests:
        target: 100%
        paths:
          - "tests/"
      package:
        target: 60%
        paths:
          - "!tests/"
    patch:
      default:
        target: 80%
